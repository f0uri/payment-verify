# 📤 رفع المشروع على GitHub وبناء التطبيق تلقائياً — من الهاتف فقط

هذا الدليل مكتوب لمن **يملك هاتفاً فقط**. اتبع الخطوات كما هي بالترتيب (5 دقائق).

---

## الطريقة الأسهل: ملفان فقط ثم زر واحد ✅

GitHub Actions سيبني الـ APK نيابة عنك ويعطيك ملف `payment-verify.apk` جاهزاً للتنصيب.

### 1️⃣ أنشئ مستودعاً جديداً
1. افتح **github.com** من متصفح الهاتف وسجّل دخولك.
2. اضغط زر **＋** (أعلى اليمين) ← **New repository**.
3. الاسم مثلاً: `payment-verify` — اختر **Private** أو **Public**.
4. اتركه فارغاً (بدون README) واضغط **Create repository**.

### 2️⃣ ارفع ملف البذرة (يحوي كل مصادر التطبيق)
1. من صفحة المستودع: **Add file** ← **Upload files**.
2. اختر من مجلد العمل الملف: **`android-sources.tar.gz.b64`** ← Commit changes.

### 3️⃣ أنشئ ملف الأكشن
1. **Add file** ← **Create new file**.
2. في خانة الاسم اكتب بالضبط (الشرطة المائلة تنشئ المجلدات تلقائياً):
   ```
   .github/workflows/android.yml
   ```
3. الصق محتوى الملف **`.github/workflows/android.yml`** الموجود في مجلد العمل (من مدير الملفات افتحه كنص وانسخه).
4. اضغط **Commit changes**.

### 4️⃣ شغّل البناء واحصل على APK
1. افتح تبويب **Actions** في المستودع.
2. ستجد «Build Android APK» يعمل تلقائياً (أو شغّله يدوياً: **Run workflow**).
3. بعد ~دقيقة اضغط على التشغيل ← نزله لقسم **Artifacts** ← حمّل **payment-verify-apk**.
4. فك الضغط ← افتح `payment-verify.apk` ← اسمح بالتنصيب من مصادر غير معروفة إن طُلب منك. 🎉

> 💡 **ترقيم إصدار + ريليز:** من تبويب **Releases** اختر Draft a new release واكتب tag مثل `v1.0` — سيرفع الأكشن الـ APK تلقائياً في صفحة ريليز دائمة.

---

## طريقة بديلة: رفع المشروع كاملاً (للمستقبل)

عندما يتوفر لديك حاسوب (أو عبر تطبيق **Termux** على أندرويد: `pkg install git` ثم أوامر git):

- الملف **`payment-verify-github.zip`** يحوي المشروع كاملاً (الخادم + التطبيق + الأكشن).
- بعد فك الضغط ورفع كل المجلدات، سيبني الأكشن الـ APK **من مصادر المستودع مباشرة**
  (إن وُجد مجلد `android/` يتجاهل ملف البذرة تلقائياً).

### تحديث التطبيق لاحقاً (الطريقة السريعة)
بعد أي تعديل، أعِد توليد البذرة من داخل مجلد `android`:
```bash
tar -czf - --exclude='build' --exclude='*.apk' android/ | base64 -w0 > android-sources.tar.gz.b64
```
ثم ارفع النسخة الجديدة فوق القديمة (Add file ← Upload files) وسيُعاد البناء تلقائياً.

---

## ⚙️ ماذا يفعل الأكشن؟
| الخطوة | التفاصيل |
|---|---|
| Java 17 | عبر `actions/setup-java` |
| Android SDK | المعطيات المسبقة على العدّاء + تثبيت الناقص تلقائياً (platform-34, build-tools 34.0.0) |
| البناء | `aapt2 → javac → d8 → zipalign → apksigner` (نفس السكربت المحلي) |
| المخرجات | Artifact باسم `payment-verify-apk` + مرفق في Release عند tag يبدأ بـ `v` |

الملف المبني **موقّع بنفس مفتاح الديبو** الموجود في البذرة (`debug.keystore`) — أي أن التحديث فوق نسخة سابقة مثبتة على الهاتف سيعمل بدون حذف التطبيق.

> 🔐 ملاحظة أمان: المفتاح مفتاح تطوير (debug) وليس سرّاً حساساً؛ عند النشر في المتاجر لاحقاً أنشئ مفتاح إصدار خاصاً بك.
